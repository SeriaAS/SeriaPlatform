This component is simply a wrapper around S. C. Chens excellent PHP Simple HTML DOM Parser

See http://sourceforge.net/projects/simplehtmldom/ for more information



Usage: 

$dom = SimpleHtmlDom::fetch($url)		accepts an URL or a file
$dom = SimpleHtmlDom::parse($html)		accepts a HTML string
